@require '../constants'

.p
  font-family: sansserif
  font-size: var(--size);
  font-weight: var(--weight);
  line-height: 20px
  margin: 0
  + .p
    margin-bottom: 1em
.defaultMargin
  margin: 1em 0